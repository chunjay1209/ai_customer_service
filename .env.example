# 数据库文件路径（默认 SQLite，放在 data/app.db）
DB_URL=sqlite:///./data/app.db

# JWT 签名密钥（部署到生产环境请改为长随机字符串）
JWT_SECRET=please-change-me-to-a-long-random-string

# 后端 API 基地址（Streamlit 前端使用）
API_BASE_URL=http://127.0.0.1:8000
